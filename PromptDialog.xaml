<Window x:Class="Kater1EQ.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kater1EQ" Height="160" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource BgCard}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText" Text="Tên preset:" Grid.Row="0"
                   Foreground="{DynamicResource TextPrimary}" Margin="0,0,0,10"/>

        <TextBox x:Name="InputTextBox" Grid.Row="1" Padding="8,6"
                 Background="{DynamicResource BgElevated}"
                 Foreground="{DynamicResource TextPrimary}"
                 BorderBrush="{DynamicResource BorderColor}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Huỷ" Style="{DynamicResource SecondaryButton}" Width="70" Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button Content="Lưu" Style="{DynamicResource PrimaryButton}" Width="70"
                    Click="Ok_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
